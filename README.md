# artemis

第一个仓库。

目前放着 [`claude-kit/`](./claude-kit) —— 一套让 Claude Code 更靠谱的配置：
改完代码立刻自检、拦截会卡死会话的命令、把「犯过的错」变成执行不了的硬规则、
收工前自动验证。

```bash
cd claude-kit && ./install.sh
```

细节见 [claude-kit/README.md](./claude-kit/README.md)。
