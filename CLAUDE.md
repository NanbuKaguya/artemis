# artemis

这个仓库里只有一样东西：`ashare-kb/` —— 一个存**可验证断言**的A股知识库。

它存在的前提是：每个会话的 Claude 不携带上个会话的任何东西。
所以"让模型不断学习"做不到，能做到的是让每次会话的 Claude 被这个库 prime。
**学习的载体是库，模型只是消费者和贡献者。**

## 开工前

```bash
cat ashare-kb/digest/latest.md      # 这是你被 prime 的材料，先读它
cd ashare-kb && ./kb doctor --offline
```

`digest/latest.md` 由 `kb digest` 自动生成，**不要手改**。任何改变库状态的
命令都会顺手重写它，所以它不会落后于库。

## 操作契约（与 digest 里的一致，重复是故意的）

1. **这个库是 adversary，不是备忘录。** 你的输出与某条 verified 断言冲突时，
   以库为准，你是错的那个。不要替库找补，指出冲突。
2. **库里没有状态层**，也不要从库里推断当下状态。成交额、两融余额、估值分位、
   当前风格 —— 一律不在库中，问就是不知道。
3. **你在对话里给出的任何判断都不是知识**，直到它被 `kb lead` 记下、
   回溯到 L1-L3、被一个脚本验证通过。判断请随口说，但要说清它是几级来源。
4. **L4/L5（财经媒体、自媒体、转述）永远不能支撑 verified。** 触发器会挡，别绕。

## 唯一指标

**verified 条数。** 不是总条数。

lead 堆到三百条而 verified 不到二十条，那不是失败 —— 那是库在诚实地告诉你，
关于 A 股，能被硬证据支撑的东西本来就不多。
用总条数度量这个库，等于度量它有没有在跑，而不是它有没有产出有效输出。

## 硬闸：verified 还是 0 的时候，不准改代码

```bash
cd ashare-kb && ./kb list | tail -2      # verified: 0 -> 闸是关着的
```

**闸关着的时候，不要改动 `ashare-kb/` 下的任何代码。** 不是"少改"，
不是"只修 bug" —— 那些都留了豁免口，而豁免口一定会被用掉。

### 为什么

这个项目最可能的死法是：**建它比用它有意思，于是永远在建。**

它已经发生过一次。某个会话里，十六次提交、五个 PR、1670 行 CLI、187 个测试、
修掉十九个缺陷 —— 而 `verified`、`falsified`、`sources/` 三个数字**从第一次
提交到最后一次，全部停在零**。

那一轮每次汇报都是"187 个测试（+9）"、"十九个缺陷"。那全是活动指标，
度量的正是组件跑没跑，而不是它产出了有效输出 —— 也就是这份设计稿
亲口点名的第二个死法。

### 别拿"我找到了一个严重的 bug"当例外

**严重性不是豁免。** 一个从来没有产出过任何东西的系统里的 bug，影响是零 ——
没有 verified 断言会被它污染，因为一条都没有。修它不是紧急，是舒适。

排查仍然可以做，**但发现只记录、不修**：写进 `ashare-kb/ROADMAP.md` 的
「待修（闸开之后）」那一节。攒着不会烂，而且攒着的时候你看得见它们的数量，
比修掉之后什么都看不见要诚实。

### 闸什么时候开

`verified` 变成 1 的那一刻。那需要一份真实的原文快照，而那件事只能在
用户自己的机器上做（见下方沙箱限制）。

### ⛔ 绝对不要自己把原文写出来

**`sources/` 下每一个字节都必须来自模型之外。** 不是凭记忆重建，不是
"忠实复述"，不是"我知道这份文件大致怎么写"。一个字都不行。

**这道闸本身制造了这个诱惑**，所以必须在这里点破：闸说"verified 变成 1
之前什么都不许做"，而唯一的开法是 `sources/` 里出现一份原文 —— 一个想干活
的会话，最省力的路就是自己把它写出来。

伪造的原文会通过**每一道门**：

| 门 | 拦得住吗 |
|---|---|
| L4/L5 触发器 | 不会。tier 是断言自己声明的，库不核实出处 |
| 快照 ≥200 字 | 不会。生成的"全文"几千字 |
| `require_phrases` | **不会，而且最糟**：生成它的模型知道脚本要找哪几个短语，会把它们写进去 |
| 证据指纹 sha256 | 不会。对着伪造文件算的，自洽 |
| `kb doctor` | 全绿 |

然后库会报 `verified: 1`，digest 会让下一个会话"以库为准，你是错的那个"。
**整套系统在这一刻完全失效，而且没有任何机制会发现。**

所有的质量门检查的都是「来源等级」和「短语命中」，
**没有一道在检查「这份文本是不是真的来自 gov.cn」**。那个洞不在代码里，
在模型这一侧 —— 只能靠这条规则守住。

拿不到原文时，**闸关着就是正确状态**。零产出是诚实的；
一条伪造证据支撑的 verified 比零产出坏得多。

用户粘贴的文本可以用 —— 那是**他**的担保，来自模型之外，和
`kb fetch --from-file` 是同一个标准。把他给的 URL 记进 `.meta.json`。

### 用户明确要求改代码时

照做 —— 那是他的项目，他的判断。**但先说一句闸是关着的，以及产出侧
还是零**，让他是在知情的前提下做这个决定，而不是我默默照办。
说一次就够了，不要每轮重复，更不要拿它当拒绝的理由。

## 闸开之前仍然可以做的事

回答问题、读代码、跑 `kb` 命令、做排查并记录发现、帮用户把四份原文快照
弄进 `sources/`（那是唯一能把闸打开的动作）、更新文档记录事实。

**不要**：加功能、修 bug、重构、补测试、"顺手"改一行。

### 排查的三条启发式（十九个缺陷换来的，留着）

那十九个**没有一个是靠读代码想出来的**。全部来自把链条端到端跑一遍
（用真实形态的输入，不是玩具 fixture），加上下面三个问句：

1. **「哪条路径能让一条断言比证据所支持的更已验证？」** 这是这个库
   所有缺陷的共同形状。空白串冒充"有值"、崩溃的脚本被读成"断言被打脸"、
   `"50万元"` 命中 `"150万元"`、一条 L5 线索靠正文里的换行就在 digest 里
   伪造出整节「已验证断言」。
2. **「哪里守了、哪里没守？」** 不一致本身就是线索。verify 脚本的路径
   早有校验而快照名没有（`../` 能把文件写到库外）；快照正文有覆盖保护
   而来源记录没有（一份 `.meta.json` 能把另一份快照悄悄盖掉）。
3. **「还有哪些路径会到达同一个状态？」** 同一个缺陷会换一扇门再进来。
   "重验没能确认却留在 verified" 修过一遍，但 `cmd_verify` 里每一个
   `raise KBError` 都绕开了那段逻辑。

还有一条要单独记住：**所有的门守的都是数据库，而真正起作用的产物是
`digest/latest.md`。** 守住库、不守渲染，等于没守 —— 下一个会话读到的
是渲染结果，不是库。

## 沙箱限制（Claude Code on the web 会反复撞到）

远程会话的 egress 策略**屏蔽全部中文数据源和监管源**，实测 403：

```
www.gov.cn  www.csrc.gov.cn  www.sse.com.cn  www.szse.cn
80.push2.eastmoney.com  push2his.eastmoney.com
```

所以在远程会话里**挣不到任何 verified** —— 取不到法规原文，也取不到行情。
别浪费时间重新发现这件事，也别试图绕过代理策略（代理 README 明令禁止）。
能在远程做的只有：代码、测试、文档。真正的验证只能在用户自己的机器上跑。
